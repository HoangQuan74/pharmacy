export enum TeachingStatus {
    retire = 0,
    teaching = 1,
    pause = 2,
    quit_job = 3,
}

export enum accountStatus {
    disabled = 0,
    Enabled = 1,
}

export enum studyingStatus {
    suspension = 0,
    studying = 1,
    deferral = 2,
    graduate = 3,
}

export enum status {
    failed = 0,
    finished = 1,
    processing = 2,
}

export enum statusFinished {
    outOfExpire = 0,
    onTime = 1,
}

export enum role {
    admin = 'admin',
    teacher = 'teacher',
    student = 'student',
    business = 'business',
}