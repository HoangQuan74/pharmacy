export enum Gender {
    FEMALE = 'female',
    MALE = 'male',
    OTHER = 'other',
}

export enum typeChannel {
    DIRECT = 'direct',
    GROUP = 'group',
}

export enum MemberRoles {
  ADMIN = "admin",
  MEMBER = "member",
}